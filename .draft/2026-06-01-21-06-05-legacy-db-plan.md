Objective

- Locate the legacy SQLite databases in /Users/xiewendao/Downloads that correspond to the screenshot content.
- Normalize the matched filenames by removing any trailing space-plus-number suffix before the .db extension.
- Move the normalized databases into /Users/xiewendao/.polywise/db.
- Inspect packages/polywise/drizzle migrations and update the target database migration state directly so the moved databases do not error on startup.

Execution plan

1. Read the package-level route files and samples required for changes under packages/polywise/drizzle.
2. Inventory legacy .db files under /Users/xiewendao/Downloads and inspect their contents for the screenshot article/title markers.
3. Identify the two matching databases and confirm the rename pattern needed.
4. Inspect the destination db folder and the existing drizzle migration files / metadata expectations.
5. Move the matched databases with normalized names into /Users/xiewendao/.polywise/db.
6. Modify the moved databases' migration bookkeeping directly to align with the current drizzle migration and prevent runtime errors.
7. Re-verify the final filenames, locations, and migration rows.

Progress note

- Identified screenshot match in app.db 22.45.58 with paired vec.db 22.45.58.
- Confirmed current migration is a squashed full-schema migration; legacy schema matches structurally, so the safe fix is to seed \_\_drizzle_migrations with the current migration name after moving the DB.

Progress note

- Backed up matched legacy app/vec databases into /Users/xiewendao/.polywise/db as app.db and vec.db.
- Seeded \_\_drizzle_migrations in the destination app.db with the current squashed migration name to prevent duplicate schema application.
- Removed the matched legacy source files from /Users/xiewendao/Downloads.

Verification note

- Destination app.db contains the screenshot article title and the injected 20260601032656_nasty_bloodscream migration row.
- A direct drizzle-orm migrator run against a temporary copy of the destination app.db completed with migrate-ok.
