# Do not modify!
# This file was generated by https://github.com/JLarky/gha-ts

name: Release
"on": 
  workflow_dispatch: 
    inputs: 
      version_number: 
        description: Optional release version in *.*.* format. Leave empty to auto-increment from packages/polywise/package.json.
        required: false
        type: string
concurrency: 
  group: "release-${{ github.workflow }}-${{ github.run_id }}"
  cancel-in-progress: false
permissions: 
  contents: write
  id-token: write
jobs: 
  prepare: 
    uses: ./.github/workflows/prepare.generated.yml
    with: 
      version_number: "${{ github.event.inputs.version_number }}"
    secrets: inherit
  standalone: 
    needs: 
      - prepare
    uses: ./.github/workflows/standalone.generated.yml
    with: 
      release_version: "${{ needs.prepare.outputs.release_version }}"
      release_tag: "${{ needs.prepare.outputs.release_tag }}"
    secrets: inherit
  desktop: 
    needs: 
      - prepare
      - standalone
    uses: ./.github/workflows/desktop.generated.yml
    with: 
      release_version: "${{ needs.prepare.outputs.release_version }}"
      release_tag: "${{ needs.prepare.outputs.release_tag }}"
      release_commit: "${{ needs.prepare.outputs.release_commit }}"
    secrets: inherit
