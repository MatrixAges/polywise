The user wants to create project skeletons for 'polywise' and 'chaosbench' inside 'packages/'.
The requirements are:

- Follow erpc's rslib configuration.
- Add package.json.
- Create src/index.ts.
- ESM format only.
