# User Request - 2026-02-01 11:07:25

Target: Reference the following conversation and work with me to implement the core basic memory function of polywise:
Using two tables + recursive SQL to implement a graph database, can it simulate the neural network structure of the human brain:

- Node represents nerve cells, it has "size" (the higher the activation frequency, the larger it is).
- Edge represents nerve connections, it has thickness (the more frequent the simultaneous activation between two nodes, the thicker it is) and length (distance, adjusted according to the neural connection mechanism).
  Please help me simulate the above functions.

---

Conversation context (summarized):

- Suggests using PGlite for local SQL.
- Schema: `neurons` (id, label, x, y, potential, activation, threshold, last_fired_at) and `synapses` (id, source_id, target_id, weight, distance).
- Core logic: `tick()` function with stages:
     1. Propagation: Calculate incoming signals.
     2. Activation: Check potential against threshold.
     3. Hebbian Learning: Adjust weight (wire together, fire together).
     4. Structural Plasticity: Adjust distance based on weight.
- Visualization: Node size = activation/weight, Edge thickness = weight, Edge length = distance.

Task: Install pglite, write the code, and write test code that can be run from the command line.
