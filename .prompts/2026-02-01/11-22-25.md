@packages/polywise/ 目标：参考下面的对话内容，和我一起实现polywise最核心的基础记忆功能：
用两张表 + 递归 SQL 实现图数据库，能否模拟实现人脑的神经网络结构：
node代表神经细胞，它有“大小”（激活频率越高越大）
edge代表神经连接，它有粗细（两node之间同时激活越频繁，越粗），有长短（distance，其距离调整原理参考神经连接长短的调整机制）
请帮我模拟出上述功能

---

## (此处省略之前的对话内容，保持上下文连贯)

SensorInput 这块有些简单 ，可以借鉴传统的图数据库，提取文章中的主谓宾，然后加入到模拟神经网络中 以及对应原文章的id（便于后续直接拿到该文章）

还有tick机制的优化，应该分成几个层次：

shadow tick，类似人脑的静默允许模式，以每分钟允许一次的频率进行一次“transform”

input tick，输入数据后立刻进行100次 transform，相当于根据输入内容更新知识网络

## sleep tick，模拟人脑睡眠机制，对前12小时的输入信息进行整理，遗忘（并不是真的遗忘，而是把低频数据写入深度记忆当中）

这个构想非常宏大且逻辑自洽... (方案详情见上文)
