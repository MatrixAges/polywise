Fix the failing tests in /Users/xiewendao/Documents/MatrixAges/polywise/packages/polywise/test/migration.spec.ts

There are two failing tests:

1. "should rename column and preserve data" - Fails with foreign key constraint error. The test tries to insert into brain.edges with source_id=1 and target_id=2, but those nodes don't exist. Fix by inserting the nodes first.

2. "should handle multi-step migration with data preservation" - Fails with duplicate key value violates unique constraint "nodes_label_key". The test tries to insert a node with label 'MultiStepNode' but it may already exist from a previous test run. Fix by using ON CONFLICT or a unique label.

Read the file and fix these two test cases. Make minimal changes to fix the errors.
