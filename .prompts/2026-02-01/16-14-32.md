The test "should modify column type with USING clause" is still failing with error "column 'weight' does not exist".

Looking at the test file /Users/xiewendao/Documents/MatrixAges/polywise/packages/polywise/test/migration.spec.ts:

1. The "Rename Column" test renames the `weight` column to `strength`
2. The "Modify Column Type" test then tries to use the `weight` column but it no longer exists

Both tests are in the same describe block and share the same database instance.

Fix this by either:

- Making each test use a fresh database (preferred), OR
- Using a different column in the "Modify Column Type" test

Apply the minimal fix needed to make all tests pass.
