Fix the formatting (line breaks/spacing) in the test files at /Users/xiewendao/Documents/MatrixAges/polywise/packages/polywise/test/

Files to fix:

1. migration.spec.ts
2. test.spec.ts

Check for:

- Missing blank lines between different execution styles (data fetching vs return, variable calculation vs usage, etc.)
- Any obvious formatting issues

Apply minimal fixes to improve readability following the code spacing rules in .agent/rules/global.md:

- Use blank lines to separate code with different execution styles
- Add blank line between data fetching and return statements
- Add blank line between variable calculation and usage

Do NOT remove the function return type annotations like `Promise<T[]>` as these are needed for the test helper functions. Focus only on spacing/line breaks.
