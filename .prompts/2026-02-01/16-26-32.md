Fix the three failing tests in /Users/xiewendao/Documents/MatrixAges/polywise/packages/polywise/test/migration.spec.ts:

1. **Line 125: "should add new column with default value"**
      - Error: duplicate key value violates unique constraint "nodes_label_key"
      - Fix: Use a unique label like `TestNode_${Date.now()}` instead of 'TestNode'

2. **Line 190: "should rename column and preserve data"**
      - Error: column "weight" does not exist (because it was renamed in previous test)
      - Fix: The test already inserts nodes and edges correctly. The issue is that the `beforeAll` only runs once per describe block. We need to ensure this test creates a fresh edge.

3. **Line 244: "should modify column type with USING clause"**
      - Error: column "weight" does not exist
      - Fix: The test tries to migrate existing edges but there are none in this database. Insert a test edge first before the migration.

Apply these fixes to make all tests pass. Be minimal with changes.
