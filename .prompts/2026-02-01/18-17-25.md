Update the Polywise.ts file to support the new fields (idol_id, root_ids, metrics_ids) for nodes and edges.

The file is located at /Users/xiewendao/Documents/MatrixAges/polywise/packages/polywise/src/Polywise.ts

Here are the changes needed:

1. Update `addNode` method to accept optional idol_id, root_ids, metrics_ids parameters
2. Update `connect` method to accept optional idol_id, root_ids, metrics_ids parameters
3. Update `getAllNodes` to include new fields in SELECT
4. Update `upsert_node` method to accept optional idol_id, root_ids, metrics_ids parameters
5. Update `processArticle` method to accept optional filtering parameters and pass them to inject_triples
6. Update `inject_triples` method to accept optional filtering parameters and pass them to SQL functions
7. Add new query methods for filtering:
      - getNodesByIdol(idol_id: string)
      - getNodesByRoot(root_id: string)
      - getEdgesByIdol(idol_id: string)
      - getEdgesByRoot(root_id: string)

Important: Make sure to maintain backward compatibility by making all new parameters optional with default values. The sql queries have been updated to support these fields already.

Return the complete updated file content.
