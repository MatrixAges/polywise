polywise 支持全文检索和向量检索支持：

- 存储文章原始文本（用于全文检索）
- 存储文章的向量

import { pipeline, env } from '@huggingface/transformers';

// 可选：手动指定模型缓存路径（默认在 ~/.cache 目录下）

import { pipeline, env } from '@huggingface/transformers';

async function getEmbedding(text, userId) {
// 动态修改存储路径（例如按用户隔离模型缓存，虽然不常用）,
env.cacheDir = app.getPath('documents')/.Polywise/models;

    // 执行这一行时，库会读取当前的 env.cacheDir
    const extractor = await pipeline('feature-extraction', 'onnx-community/Qwen3-Embedding-0.6B-ONNX', {
        dtype: 'q8'
    });

    return await extractor(text, { pooling: 'mean', normalize: true });

}

注意cacheDir，是外部（在electron的主进程中调用polywise时传入目录）传入的目录
