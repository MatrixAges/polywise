The user wants to fix naming conventions, parameter ordering, and spacing in `packages/polywise/src/`.

- Variable names must be `snake_case`.
- Parameter objects must follow the order: Required Variables > Optional Variables > Required Functions > Optional Functions.
- Blank lines should separate code with different execution styles.
- Class methods should be camelCase.
- No return type annotations (let TS infer).
- Classes must be default exported.
- Utils must follow the specific export pattern.
