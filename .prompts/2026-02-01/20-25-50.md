Please optimize the code in packages/polywise/src/Article.ts, packages/polywise/src/Brain.ts, packages/polywise/src/Polywise.ts, and packages/polywise/src/utils/embedding.ts according to Polywise standards:

1. All variable names use Rust-style snake_case.
2. All functions use camelCase.
3. Blank lines separate code context for better readability.
4. No return type annotations.
5. Classes are default exported.
6. Utils follow the 'export default () => {}' pattern without return types.
7. Parameter objects for functions/constructors with >2 parameters follow the order: Required Variables > Optional Variables > Required Functions > Optional Functions.
