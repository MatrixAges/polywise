The user wants to fix an issue where `PGlite` in `packages/polywise/src/Polywise.ts` might create the database folder in the project root if run from there. The fix is to use `process.cwd()` to ensure it's relative to a fixed location or to correctly handle the path.

Actually, looking at the instruction, it says "new PGlite时，未加入 process.cwd固定当前工作区". This suggests I should use `path.join(process.cwd(), ...)` or similar when initializing `PGlite`.

Wait, usually `PGlite` takes a path. If I want it to be in a specific place, I should probably make it relative to the package or a specific data dir.

Let's first read `agentmap.md` as per the "Read Agentmap (CRITICAL)" rule.
