The user wants to run tests in `packages/polywise` and then delete folders matching `polywise_*` in the same directory using `glob`.

I will:

1. Read `agentmap.md`.
2. Run the tests in `packages/polywise`.
3. List the files in `packages/polywise` to identify `polywise_*` folders.
4. Delete those folders.
