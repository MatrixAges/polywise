The user wants to add an `afterAll` hook to the test files in `packages/polywise` to automatically delete any folders matching `:polywise_*` using `glob` after the tests complete.

I will:

1. Read the test files in `packages/polywise/test/` to see where to add the `afterAll` hook.
2. Check `packages/polywise/package.json` to see available dependencies (e.g., `glob`, `fs-extra`).
3. Modify the test files to include the `afterAll` hook.
4. Update `agentmap.md` if necessary.
