The user noticed that the `afterAll` cleanup logic is duplicated across multiple spec files and that some `:polywise_*` folders are still not being deleted correctly.

I will:

1. Create a `packages/polywise/test/utils.ts` file to abstract the cleanup logic.
2. Update the `afterAll` hooks in all spec files to use this utility.
3. Improve the cleanup logic to ensure all matching folders are deleted (e.g., using more robust path resolution and ensuring all `polywise` instances are closed).
4. Verify why some folders remain (possibly because the process is still holding a handle or the path pattern is slightly off).
5. Run the tests again to verify everything is cleaned up.
