The user wants to use `fs-extra` instead of the native `fs` module in the test cleanup utility.

I will:

1. Add `fs-extra` and its types to `packages/polywise` devDependencies.
2. Update `packages/polywise/test/utils.ts` to use `fs-extra`.
3. Update any other test files that use `fs` if applicable (though they should mostly be using the utility now).
4. Run tests to verify.
