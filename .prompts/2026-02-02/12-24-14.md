@packages/polywise/src/Polywise.ts
constructor(params: PolywiseParams = {}) {
this.db = new PGlite(params.data_dir || ':polywise:', {
relaxedDurability: true,
extensions: { vector }
})

    	this.article = new Article({
    		db: this.db,
    		embedding_cache_dir: params.embedding_cache_dir
    	})

    	this.brain = new Brain({
    		poly: this,
    		onTick: params.onTick
    	})
    }

几个代码风格问题：

- 函数对象参数统一叫 args，类型叫做 \*Args
- 使用函数对象参数时，使用解构 const {} = args
  上面代码风格同步到 rules，强制执行，修改polywise中现有代码以符合上述规范
