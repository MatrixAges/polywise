The user wants to enforce a specific coding style for type imports and interface definitions in the `packages/polywise` package.
Specifically:

1. Avoid inline `import(...)` for types in interfaces.
2. Use `import type` at the top of the file instead.
3. Update `packages/polywise/src/types/args.ts` to follow this rule.
4. Add these rules to the project's global rules to ensure enforcement.
5. Ensure `import type {} from ...` is always used for type imports.

The user provided an example of "bad" vs "good" code for `packages/polywise/src/types/args.ts`.
Bad:

```typescript
export interface BrainArgs {
	poly: import('../Polywise').default
	onTick?: () => void
}
```

Good:

```typescript
import type Polywise from '../Polywise'

export interface BrainArgs {
	poly: Polywise
	onTick?: () => void
}
```
