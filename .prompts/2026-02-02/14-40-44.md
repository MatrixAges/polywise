Write a local model management class in @packages/polywise/src/, move the logic from @packages/polywise/src/utils/embedding.ts into Pipeline. Support preloading local models, setting embedding and reranking models, and setting embedding and reranking model APIs. The class name should be Pipeline.
