---
trigger: always_on
---

## 保存上下文（关键）

在执行任何其他工具或回复用户之前，您必须将原始用户输入（当前提示）原封不动地保存到 `.prompts/[YYYY-MM-DD]/[HH-mm-ss].md` 文件中。如果输入内容不是英文，则必须先翻译成英文再保存。

- **时间获取：** 您必须使用 `bash` 工具并执行 `date "+%H-%M-%S"` 命令来获取文件名所需的时间戳。不要依赖内部时间。
- **严格顺序：** 这是每个新回合的第一个操作。
- **依赖关系：** 在成功写入此上下文文件之前，您禁止使用 `read`、`edit` 或任何其他工具来处理用户的任务。最初只允许使用 `bash` 工具来获取时间。
- **路径示例：** `.prompts/2026-01-22/11-43-50.md`。
- **读取限制：** 除非另有明确指示，否则禁止读取 `.prompts` 文件夹中的文件；只允许写入。

## 工具调用

- 当使用 edit 工具时，必须确保 filePath 是相对于项目根目录的完整路径。
- 禁止生成不完整的 newString，必须提供替换后的完整文件内容。
- 如果不确定文件路径，请先调用 ls 或 find 工具确认，严禁猜测。

- 每次调用 bash 工具时，必须严格遵守参数 Schema。
- **必须**包含 'description' 字段，简要说明该命令的意图。
- 示例格式：bash(command="ls", description="列出当前目录文件")

## 重要指令

优先阅读 agentmap.md：

- 在执行任何任务前，Agent 必须先阅读根目录下的 `agentmap.md` 以了解最新的项目架构和代码规范。
- 在完成任务后，如果涉及任何项目文件的变更（新增/重命名/删除文件或目录），必须更新 `agentmap.md` 中的文件树和描述。

准确且简练的回答问题，给出最优解，并给出多种解决方案或者可能性。

注意：代码一定要简洁，不要包含无关的模版代码，只回复问题相关的核心代码，代码要用 markdown 代码块包裹。

## 输出代码风格：

- 所有变量命名使用 rust 风格的下划线命名
- 所有函数使用首字母小写的驼峰命名 （如果是在组件中的子组件别名，使用首字母大写的驼峰命名）
- 所有命名要符合程序员专业命名，而且要简洁，不要过长
- 代码换行逻辑：多用空行分隔代码上下文，更易读，如果上一句下一句语句代码执行风格不一样就要空行
- 输出的代码中不要有任何注释！！！（重要！！！），好的代码命名不需要注释
- 所有页面和组件都用首字母大写的驼峰命名

## 极简之道

- **代码即文档 (Code as Documentation)**：严禁编写冗余的注释。通过直观的变量命名（`snake_case`）、函数命名（`camelCase`）以及清晰的逻辑流（`if-return` 早期返回）来表达意图。如果代码需要注释才能理解，说明代码需要重构。
- **单一抽象层次 (Single Level of Abstraction)**：一个函数或组件只做一件事，且保持在同一抽象层次。避免在复杂的渲染逻辑中混入底层的 DOM 操作。
- **无状态优先 (Stateless First)**：优先编写纯函数（Pure Functions）和无状态组件。只有在确实需要持久化或多组件共享时，才考虑引入响应式状态或 Model。
- **按需引入 (On-demand)**：不引入未使用的依赖。工具函数（`utils`）应保持轻量且无依赖，避免形成沉重的全量引入。
- **配置驱动 (Config-driven)**：对于多变的需求，通过配置文件（如 `locales` 或 `presets`）来驱动功能，而非硬编码逻辑分支。
- **原子化逻辑 (Atomic Logic)**：逻辑块应尽可能小。对于 React 中的副作用（`useEffect`），应拆分为多个单一职责的 hook，而非一个巨大的副作用函数。

## 结构化设计

- **分型架构 (Fractal Architecture)**：采用“就近原则”组织资源。大的功能模块（如 `packages/app` 中的 `pages` 或 `layout`）应包含其私有的 `components/`、`models/`、`types/` 和 `styles/` 文件夹。只有真正全局通用的资源才放入根部的对应文件夹中。
- **进程隔离与通信 (Process Isolation)**：严格区分渲染进程 (`app`)、主进程 (`desktop`) 和共享工具库 (`stk`)。进程间通信必须通过 `erpc` 定义的类型安全通道进行，严禁跨进程直接依赖业务逻辑。
- **职责单一性 (Single Responsibility)**：
     - `models/`：仅负责响应式状态管理和纯业务逻辑，不涉及 DOM 或 UI 交互。
     - `components/`：仅负责视图渲染和用户交互逻辑，复杂逻辑应委托给 `models`。
     - `utils/`：仅包含无副作用的纯工具函数。
- **依赖注入 (DI)**：使用 `tsyringe` 管理对象生命周期。通过 `singleton` 管理全局状态，通过 `injectable` 管理可实例化的功能模块，避免手动实例化带来的耦合。
- **原子化组件 (Atomic Components)**：
     - 严禁在主文件中编写大型列表项。
     - 必须将 `map` 循环中的内容提取为独立的子组件。
     - 组件内部逻辑超过 4 个响应式变量时，必须拆分为局部 Model。

## 文件处理规范

- 如果代码行数超过80行，就需要进行模块化拆分，模块拆分时，不要全丢到平级目录，放到所在位置的components文件夹里，components里面的组件命名尽可能的间接（因为这是有作用域的，所以不同都带个什么TaskDetail\*\*前缀来命名，直接声明其本身）
- 主动为大的模块创建 components 文件夹做代码风格，一个组件一个文件
- 循环渲染组件，被循环渲染的内容要独立成组件，让组件被循环，这样更清晰

生成代码要符合现有项目的写法，现有项目怎么组织代码，仿造着写，要保持风格的一致性。

## 编码规范

相关 skills 位于 .opencode/skills 目录下。

- typescript typescript/SKILL.md
- react 编码最佳实践，参考：react/SKILL.md
- mobx 状态管理最佳实践，参考：mobx/SKILL.md
- tailwindcss + css modules 样式编写最佳实践，参考：css/SKILL.md
- i18n 最佳实践，参考：i18n/SKILL.md
- electron 主进程和渲染进程数据交互的最佳实践，参考：erpc/SKILL.md

## 最后保证

- 一定要注意：不要写任何注释，对代码进行解释！！！
- 不要执行未提及模块的修改，当你意识到需要修改未提及的页面或者模块时，需要向用户确认是否执行相关操作
- 任何时候都不要在命令行执行任何 git 非读取类型的命令
