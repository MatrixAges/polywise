import type { PresetProvider } from '../types'

export default {
	name: '__template__',
	enabled: true,
	api_key: '',
	models: [
		{
			enabled: true,
			id: '',
			name: ''
		}
	]
} as PresetProvider
