import type { PresetProvider } from '@core/types'

export default {
	name: '__template__',
	enabled: true,
	apiKey: '',
	models: [
		{
			enabled: true,
			id: '',
			name: ''
		}
	]
} as PresetProvider
