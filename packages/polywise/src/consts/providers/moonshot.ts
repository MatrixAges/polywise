import type { PresetProvider } from '@core/types'

export default {
	name: 'moonshot',
	enabled: true,
	apiKey: '',
	models: [
		{
			enabled: true,
			id: 'kimi-k2.5',
			name: 'Kimi k2.5'
		}
	]
} as PresetProvider
