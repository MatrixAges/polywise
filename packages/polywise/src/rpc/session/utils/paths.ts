import path from 'path'
import { app } from '@core/consts'

export const pin_path = path.resolve(app.app_path, 'pin.json')
export const session_group_path = path.resolve(app.app_path, 'session_group.json')
