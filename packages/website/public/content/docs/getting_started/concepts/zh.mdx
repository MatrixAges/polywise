# 示例文档：getting / started/concepts

这是 **Polywise** 官网的占位文档页面，用于在首版中保留参考站的阅读结构、目录结构与版式表现，后续会替换为正式内容。

## 本页说明

- getting_started/concepts 能力的占位说明
- 它在本地优先内容系统中的预期位置
- 后续正式文档会继续展开的关键概念

## 建议流程

1. 先把原始材料保存到 Polywise。
2. 再补充结构、标签和关联关系。
3. 需要检索、串联或总结时，再回到图谱、搜索与 Agent 视图。

## 备注

当前官网使用 **safe-mdx** 进行渲染，以便在 Cloudflare Workers 上安全运行。这里的内容是 mock，占位为主，后续会逐步替换。
